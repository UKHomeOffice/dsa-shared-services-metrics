apiVersion: v1
kind: Service
metadata:
  labels:
    name: dsa-shs-monitoring
  name: dsa-shs-monitoring
spec:
  ports:
  - name: prometheus-web
    port: 8000
    targetPort: 8000
  selector:
    name: dsa-shs-monitoring
